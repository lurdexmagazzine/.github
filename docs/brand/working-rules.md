# Working Rules

## Commit identity

Use the correct Git identity for the person making the change:

- **Melissa Santos**
  - `user.name`: `Melissa Santos`
  - `user.email`: `melissa@lurdex.com.br`
- **Vitoria**
  - `user.name`: `Vitoria`
  - `user.email`: `vitoria@lurdex.com.br`
- **Natani**
  - `user.name`: `Natani`
  - `user.email`: `natani@lurdex.com.br`

## Workflow notes

- Keep secrets out of git history
- Store API keys in local environment variables or a secret manager
- Never commit raw tokens, passwords, or private keys
- Keep brand work aligned with the manifesto and design system
- For product copy, prefer premium editorial language over promotional shouting

## Brand-safe defaults

- Use `R$` for prices
- Keep layouts clean and visually calm
- Prefer language that feels curated and trustworthy
